import React from "react";
import ReactDOM from 'react-dom';
import Form './Form';

const rootElement = document.getElementById("root");
ReactDOM.render(
    <React.StrictMode>
        <Form/>
    </React.StrictMode>,
    rootElement
);
